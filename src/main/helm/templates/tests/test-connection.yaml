apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "owm-dojo.fullname" . }}-test-connection"
  labels:
    {{- include "owm-dojo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "owm-dojo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
